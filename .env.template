# Domain configuration
# Replace with your actual domain (e.g., example.com)
DOMAIN_ROOT=example.com

# Email for Let's Encrypt SSL certificates
# Use a valid email for certificate expiry notifications
EMAIL=email@example.com

# Node Exporter client configuration
# Comma-separated list of client Node Exporter IPs with ports
# Format: IP:PORT,IP:PORT
# Example: 10.0.0.1:9100,10.0.0.2:9100
# Use private IPs within your VPC
NODE_EXPORTER_CLIENTS=10.0.0.1:9100,10.0.0.2:9100,10.0.0.3:9100

# Loki authentication for agent instances pushing logs
# These credentials are used by Promtail agents to push logs to Loki
# Use a strong username and password combination
LOKI_BASIC_AUTH_USER=your-username

# Password must be hashed with bcrypt and have $$ for each $ in Docker
# Example: If bcrypt outputs $2y$05$..., use $$2y$$05$$...
# Generate with: python3 -c "import bcrypt; print(bcrypt.hashpw('your-password'.encode(), bcrypt.gensalt()).decode())"
LOKI_BASIC_AUTH_PW="your-strong-password-hashed-with-bcrypt"
